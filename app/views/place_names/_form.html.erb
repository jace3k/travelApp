<%= form_for(@place_name) do |f| %>
  <% if @place_name.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@place_name.errors.count, "error") %> prohibited this place_name from being saved:</h2>

      <ul>
      <% @place_name.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
